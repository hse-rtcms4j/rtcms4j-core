spring:
  liquibase:
    enabled: true
  data:
    redis:
      host: localhost
      port: 6370

zonky:
  test:
    database:
      postgres:
        initdb:
          properties:
            locale: en_US.UTF-8
            lc-collate: en_US.UTF-8
